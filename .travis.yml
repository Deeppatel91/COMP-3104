language: node_js
node_js:
  - "14"  # Use a stable version of Node.js that is compatible
cache:
  directories:
    - node_modules
install:
  - npm install  # Install project dependencies
script:
  - npm run build  # Adjust according to your build command
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: build
  on:
    branch: main
